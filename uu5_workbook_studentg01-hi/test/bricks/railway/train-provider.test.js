import Uu5WorkbookStudent from "uu5_workbook_studentg01-hi";
import { mount, wait } from "uu5g05-test";

describe(`Uu5WorkbookStudent.Bricks.Railway.TrainProvider`, () => {
  // it("onLoad", async () => {
  //   let onLoad = jest.fn(async () => ({ content: "Hello, world!" }));
  //   let loadDtoIn = { foo: "bar" };
  //   let wrapper = mount(<Uu5WorkbookStudent.Bricks.Railway.TrainProvider onLoad={onLoad} loadDtoIn={loadDtoIn} />);
  //   expect(onLoad).toHaveBeenCalledTimes(1);
  //   expect(onLoad).lastCalledWith(loadDtoIn);
  //   await wait();
  //   expect(wrapper.text()).toContain("Hello, world!");
  // });
});
